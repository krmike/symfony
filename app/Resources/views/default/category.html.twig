{% extends 'base.html.twig' %}

{% block body %}
    <h1>welcome to the category {{ category.name }} page</h1>
    <div>
        <p>
            {{ category.description }}
        </p>
    </div>
    <div>
        {% for product in products %}
            <div>{{ product.name }}</div>
        {% endfor %}
    </div>
{% endblock %}

